name: Check mentions

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: check-mentions
  cancel-in-progress: false

jobs:
  check:
    runs-on: ubuntu-latest
    # ログインリトライ(最大3回×90秒)を含むため余裕を持たせる
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      # Playwright のブラウザをキャッシュして実行を高速化
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        id: pw-cache
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('package-lock.json') }}

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Check mentions
        run: node src/check-mentions.mjs
        env:
          SITE_EMAIL: ${{ secrets.SITE_EMAIL }}
          SITE_PASSWORD: ${{ secrets.SITE_PASSWORD }}
          LINE_CHANNEL_ACCESS_TOKEN: ${{ secrets.LINE_CHANNEL_ACCESS_TOKEN }}
          LINE_USER_ID: ${{ secrets.LINE_USER_ID }}
          MENTION_ITEM_SELECTOR: ${{ vars.MENTION_ITEM_SELECTOR }}

      # dumpDebug() が保存した失敗時の画面は runner ごと破棄されるため、
      # 後から原因(エラー文言 / reCAPTCHA / メンテ画面)を確認できるよう回収する
      - name: Upload debug snapshot
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          # 再実行時に同名 artifact で失敗しないよう attempt 番号も含める
          name: debug-${{ github.run_id }}-${{ github.run_attempt }}
          path: debug/
          if-no-files-found: ignore
          retention-days: 14

      - name: Commit state
        if: always()
        run: |
          # 60日間コミットがないと schedule が無効化されるため、50日経過で keepalive を更新
          if [ -z "$(git log -1 --format=%ct 2>/dev/null)" ] || \
             [ $(( $(date +%s) - $(git log -1 --format=%ct) )) -gt 4320000 ]; then
            date > .keepalive
            git add .keepalive
          fi
          git add state.json 2>/dev/null || true
          if ! git diff --cached --quiet; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git commit -m "Update mention state"
            git pull --rebase origin main
            git push
          fi
